'use strict';

import { StyleSheet } from 'react-native';

const styles = {
	container: {
		flex: 1,
		justifyContent: 'center',
		alignItems: 'center',
		backgroundColor: '#FFF',
	}
};

export default StyleSheet.create(styles);